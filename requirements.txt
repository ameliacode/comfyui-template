# Add your dependencies here.
# Do NOT re-list packages already bundled with ComfyUI:
#   torch, torchvision, torchaudio, numpy, Pillow, scipy,
#   safetensors, transformers, accelerate, huggingface_hub
#
# Example:
# opencv-python>=4.8.0
# aiohttp>=3.9.0
